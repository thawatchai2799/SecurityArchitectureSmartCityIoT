{
  "name": "cityanchor",
  "version": "1.0.0",
  "description": "CityAnchor chaincode: anchors alert and model digests for the smart-city security architecture",
  "main": "index.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "fabric-chaincode-node start",
    "docker:start": "fabric-chaincode-node server --chaincode-address=$CHAINCODE_SERVER_ADDRESS --chaincode-id=$CORE_CHAINCODE_ID_NAME"
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.0",
    "fabric-shim": "^2.5.0"
  }
}
